# SIE — frontend estático (Vite 5 + React SPA)
# Cloudflare: NO usar el preset "Framework: Vite" de Wrangler.
# Este proyecto despliega solo la carpeta dist/ como assets estáticos.

name = "sie"
compatibility_date = "2024-09-23"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
